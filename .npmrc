# 允许以下依赖在安装期执行构建脚本（pnpm 9+/10+ 在 CI 默认需要白名单审批）
# Allow build scripts for selected native/binary dependencies in CI/Vercel.
#
# 相关告警示例：Ignored build scripts: bufferutil, esbuild, sharp, unrs-resolver

only-built-dependencies[]=esbuild
only-built-dependencies[]=sharp
only-built-dependencies[]=bufferutil
only-built-dependencies[]=unrs-resolver
